//
// ContentView.swift
// PhotoScanner
//
// 应用首页。
// 第一阶段直接展示验证页，后续阶段替换为正式产品 Tab 结构。
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        SimilarityDebugView()
    }
}

#Preview {
    ContentView()
}
