[project]
name = "photoscanner-baseline-tools"
version = "0.1.0"
description = "Python tools for generating PhotoScanner ChineseCLIP baselines"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.2.0",
  "onnxruntime>=1.24.0",
  "pillow>=11.0.0",
  "six>=1.17.0",
  "transformers>=4.57.0",
]

[tool.uv]
package = false
